{% extends 'admin/base.html.twig' %}

{% block title %}SocialNetwork{% endblock %}

{% block body %}
    <h1>SocialNetwork</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ social_network.id }}</td>
            </tr>
            <tr>
                <th>SnName</th>
                <td>{{ social_network.snName }}</td>
            </tr>
            <tr>
                <th>SnURL</th>
                <td>{{ social_network.snURL }}</td>
            </tr>
            <tr>
                <th>SnLogo</th>
                <td>{{ social_network.snLogo }} : <i class="fa {{ social_network.snLogo }}"></i></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_social_network_index') }}">back to list</a>

    <a href="{{ path('app_admin_social_network_edit', {'id': social_network.id}) }}">edit</a>

    {{ include('admin/social_network/_delete_form.html.twig') }}
{% endblock %}
